all:
  children:
    osds:
      hosts:
        10.0.1.10:
          ansible_user: rock64
          data_disks:
            - /dev/sda1
            - /dev/sda2

    mons:
      hosts:
        10.0.1.20:
          ansible_user: ljishen

    mgrs:
      hosts:
        10.0.1.20:
          ansible_user: ljishen

    admins:
      hosts:
        localhost:
          ansible_user: ljishen
          ansible_connection: local
          ansible_python_interpreter: '{{ ansible_playbook_python }}'
